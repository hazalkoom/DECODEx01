# Dependency Guide
> **Onboarding question:** What depends on what?


The dependency graph shows how different parts of the system interact. A strong connection exists between `scripts` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `scripts` and `sys`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `scripts` and `time`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `scripts` and `codelens_core`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `scripts` and `python`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/api` and `<pybind11/pybind11`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/api` and `<pybind11/stl`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/api` and `"`, with 3 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<string>`, with 3 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<vector>`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<cstdint>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `"fs_walker`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<filesystem>`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<unordered_set>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `"language_detector`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<fstream>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<unordered_map>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/core` and `<string_view>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `"ast_parser`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `"symbol_extractor`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `"dependency_extractor`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `"reference_extractor`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `<string>`, with 4 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `<vector>`, with 4 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `<tree_sitter/api`, with 4 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `"`, with 4 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/parser` and `<string_view>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_db` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_db` and `sqlalchemy`, with 8 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_db` and ``, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_db` and `typing`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_graphs` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_graphs` and `sqlalchemy`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_graphs` and `pyvis`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src` and `sys`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src` and `time`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src` and `codelens_core`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src` and `python`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests` and `pytest`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests` and `python`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/core` and `os`, with 4 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/core` and `time`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/core` and `pytest`, with 5 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/core` and `threading`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/core` and `tempfile`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/core` and `codelens_core`, with 5 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/core` and `stat`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `<gtest/gtest`, with 4 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `<string>`, with 3 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `<vector>`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `"ast_parser`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `<fstream>`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `<algorithm>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `<cstdio>`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `<filesystem>`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `"fs_walker`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/cpp` and `"language_detector`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/db` and `pytest`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/db` and `sqlalchemy`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/db` and `python`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/graphs` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/graphs` and `python`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/integration` and `sys`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/integration` and `os`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/integration` and `python`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/integration` and `tempfile`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/integration` and `codelens_core`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/parser` and `pytest`, with 9 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/parser` and `textwrap`, with 8 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/parser` and `codelens_core`, with 11 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/parser` and `hypothesis`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/snapshot` and `os`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/snapshot` and `json`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/snapshot` and `python`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `root` and `argparse`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `root` and `sys`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `root` and `subprocess`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `root` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `root` and `python`, with 6 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_db` and `collections`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs` and `os`, with 3 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs` and `json`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs` and `typing`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs` and `jinja2`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs` and `python`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs` and `openai`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs` and ``, with 12 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs/generators` and ``, with 11 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_docs/generators` and `collections`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_snapshot` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_snapshot` and `json`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_snapshot` and `datetime`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_snapshot` and `sqlalchemy`, with 2 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_snapshot` and `jinja2`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `src/python/decode_snapshot` and `python`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/docs` and `os`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/docs` and `python`, with 1 direct interactions.


The dependency graph shows how different parts of the system interact. A strong connection exists between `tests/snapshot` and `xml`, with 1 direct interactions.



---
*Generated by DECODE NLG | [← Back to Index](index.md)*