# Project Map
> **Onboarding question:** How is the repository laid out?


## Directory: `root`

The project directory structure is designed to separate concerns. In the `root` directory, you will find 8 files primarily written in Markdown, Python, Unknown, and Plain Text.

---

## Directory: `coverage_reports/cpp`

The project directory structure is designed to separate concerns. In the `coverage_reports/cpp` directory, you will find 1 files primarily written in Unknown.

---

## Directory: `coverage_reports/cpp/html`

The project directory structure is designed to separate concerns. In the `coverage_reports/cpp/html` directory, you will find 10 files primarily written in CSS, Unknown, HTML, and JavaScript.

---

## Directory: `docs`

The project directory structure is designed to separate concerns. In the `docs` directory, you will find 19 files primarily written in Markdown.

---

## Directory: `lib/bindings`

The project directory structure is designed to separate concerns. In the `lib/bindings` directory, you will find 1 files primarily written in JavaScript.

---

## Directory: `lib/tom-select`

The project directory structure is designed to separate concerns. In the `lib/tom-select` directory, you will find 2 files primarily written in CSS and JavaScript.

---

## Directory: `lib/vis-9.1.2`

The project directory structure is designed to separate concerns. In the `lib/vis-9.1.2` directory, you will find 2 files primarily written in CSS and JavaScript.

---

## Directory: `reports/2026-07-08`

The project directory structure is designed to separate concerns. In the `reports/2026-07-08` directory, you will find 1 files primarily written in Markdown.

---

## Directory: `reports/2026-07-09`

The project directory structure is designed to separate concerns. In the `reports/2026-07-09` directory, you will find 4 files primarily written in Markdown.

---

## Directory: `reports/2026-07-10`

The project directory structure is designed to separate concerns. In the `reports/2026-07-10` directory, you will find 1 files primarily written in Markdown.

---

## Directory: `reports/2026-07-11`

The project directory structure is designed to separate concerns. In the `reports/2026-07-11` directory, you will find 3 files primarily written in Markdown.

---

## Directory: `reports/2026-07-13`

The project directory structure is designed to separate concerns. In the `reports/2026-07-13` directory, you will find 4 files primarily written in Markdown.

---

## Directory: `scripts`

The project directory structure is designed to separate concerns. In the `scripts` directory, you will find 4 files primarily written in Shell and Python.

---

## Directory: `src/api`

The project directory structure is designed to separate concerns. In the `src/api` directory, you will find 1 files primarily written in C++.

---

## Directory: `src/core`

The project directory structure is designed to separate concerns. In the `src/core` directory, you will find 5 files primarily written in C++.

---

## Directory: `src/parser`

The project directory structure is designed to separate concerns. In the `src/parser` directory, you will find 8 files primarily written in C++.

---

## Directory: `src/python/decode_db`

The project directory structure is designed to separate concerns. In the `src/python/decode_db` directory, you will find 3 files primarily written in Python.

---

## Directory: `src/python/decode_docs`

The project directory structure is designed to separate concerns. In the `src/python/decode_docs` directory, you will find 4 files primarily written in Python.

---

## Directory: `src/python/decode_graphs`

The project directory structure is designed to separate concerns. In the `src/python/decode_graphs` directory, you will find 2 files primarily written in Python.

---

## Directory: `src/python/decode_graphs/templates`

The project directory structure is designed to separate concerns. In the `src/python/decode_graphs/templates` directory, you will find 1 files primarily written in HTML.

---

## Directory: `src/python/decode_snapshot`

The project directory structure is designed to separate concerns. In the `src/python/decode_snapshot` directory, you will find 2 files primarily written in Python.

---

## Directory: `src`

The project directory structure is designed to separate concerns. In the `src` directory, you will find 1 files primarily written in Python.

---

## Directory: `tests`

The project directory structure is designed to separate concerns. In the `tests` directory, you will find 1 files primarily written in Python.

---

## Directory: `tests/core`

The project directory structure is designed to separate concerns. In the `tests/core` directory, you will find 5 files primarily written in Python.

---

## Directory: `tests/cpp`

The project directory structure is designed to separate concerns. In the `tests/cpp` directory, you will find 4 files primarily written in C++.

---

## Directory: `tests/db`

The project directory structure is designed to separate concerns. In the `tests/db` directory, you will find 3 files primarily written in Python.

---

## Directory: `tests/graphs`

The project directory structure is designed to separate concerns. In the `tests/graphs` directory, you will find 1 files primarily written in Python.

---

## Directory: `tests/integration`

The project directory structure is designed to separate concerns. In the `tests/integration` directory, you will find 2 files primarily written in Python.

---

## Directory: `tests/parser`

The project directory structure is designed to separate concerns. In the `tests/parser` directory, you will find 11 files primarily written in Python.

---

## Directory: `tests/snapshot`

The project directory structure is designed to separate concerns. In the `tests/snapshot` directory, you will find 2 files primarily written in Python.

---

## Directory: `src/python/decode_docs/generators`

The project directory structure is designed to separate concerns. In the `src/python/decode_docs/generators` directory, you will find 12 files primarily written in Python.

---

## Directory: `src/python/decode_docs/templates`

The project directory structure is designed to separate concerns. In the `src/python/decode_docs/templates` directory, you will find 13 files primarily written in Unknown.

---

## Directory: `src/python/decode_snapshot/templates`

The project directory structure is designed to separate concerns. In the `src/python/decode_snapshot/templates` directory, you will find 1 files primarily written in Unknown.

---

## Directory: `tests/docs`

The project directory structure is designed to separate concerns. In the `tests/docs` directory, you will find 1 files primarily written in Python.

---


*Generated by DECODE NLG | [← Back to Index](index.md)*