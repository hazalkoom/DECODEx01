# Data Flow
> **Onboarding question:** How does information move?

{% for wf in workflows %}
## Workflow: {{ wf.ep.fqn }}

{{ wf.prose }}

```mermaid
graph TD
    Start[{{ wf.ep.fqn | replace(".", "_") }}]
{% for chain in wf.chains[:15] %}
{% if chain.caller and chain.callee %}
    {{ chain.caller | replace(".", "_") | replace("<", "") | replace(">", "") }} --> {{ chain.callee | replace(".", "_") | replace("<", "") | replace(">", "") }}
{% endif %}
{% endfor %}
```
{% endfor %}

---
*Generated by DECODE NLG | [← Back to Index](index.md)*
