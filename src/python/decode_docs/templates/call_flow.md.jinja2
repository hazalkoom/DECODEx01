# Call Flow
> **Onboarding question:** What happens during execution?

{% for flow in flows %}
## Flow from `{{ flow.ep.fqn }}`

{{ flow.prose }}

```mermaid
sequenceDiagram
    participant Start as {{ flow.ep.fqn | replace(".", "_") }}
{% for chain in flow.chains[:20] %}
{% if chain.caller and chain.callee %}
    {{ chain.caller | replace(".", "_") | replace("<", "") | replace(">", "") }}->>{{ chain.callee | replace(".", "_") | replace("<", "") | replace(">", "") }}: calls
{% endif %}
{% endfor %}
```
{% endfor %}

---
*Generated by DECODE NLG | [← Back to Index](index.md)*
